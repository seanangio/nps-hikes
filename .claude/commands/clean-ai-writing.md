---
description: Audit and rewrite content to remove AI writing patterns
---

Only apply to files in the docs/ directory. Default to detect mode (flag patterns without rewriting) unless the user explicitly requests a rewrite.

$ARGUMENTS

Read and follow the instructions in ~/.claude/skills/avoid-ai-writing/SKILL.md
