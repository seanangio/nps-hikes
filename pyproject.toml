[project]
name = "nps-hikes"
version = "1.0.0"
requires-python = ">=3.12"

[tool.black]
line-length = 88
target-version = ['py312']

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --tb=short"
